# Core data + ML stack (versions pinned to a verified, working set)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44

# Visualisation
matplotlib>=3.7
seaborn>=0.13

# Notebook
jupyter>=1.0
nbformat>=5.9

# Tests
pytest>=7.0

# NOTE: tensorflow/keras is intentionally NOT required. The original notebook
# benchmarked a small Keras net, but it is optional and was dropped here (no
# wheels for the local Python 3.14 toolchain, and XGBoost already wins). The
# package and pipeline run fully without it.
#
# macOS only: XGBoost needs the OpenMP runtime. If you hit a libomp error,
# install it with:  brew install libomp
